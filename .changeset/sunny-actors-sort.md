---
'@soldy-ui/plugins': patch
---

Icon: `width` и `height`, заданные в разметке, применяются с первой отрисовки. Раньше плагин раскладки иконки следил только за сменой размера, а размер, с которым иконку собрали (пропом или готовым `ctrl`), до стилей не доходил: иконка рисовалась размером по умолчанию, пока значение не поменяют.
