---
'@soldy-ui/theme-oren': patch
---

CheckBox: новый вид `filled` — коробка залита цветом варианта, отмеченная и частично выбранная — насыщенным цветом с белой отметкой. Вид по умолчанию получил имя `outlined` (рамка, отметка цветом варианта): его можно задать явно, `view="outlined"` выглядит так же, как чекбокс без вида. Выключенный `plain` теперь без фона: раньше у него проступала коробка вида по умолчанию.
