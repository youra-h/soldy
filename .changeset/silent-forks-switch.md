---
'@soldy-ui/theme-oren': patch
---

Иконка в слоте по умолчанию у Button стоит по центру кнопки, а не у её начала: так прижимался к краю крестик Popover. Строки ListBox и Select держат содержимое у начала строки, заголовок Accordion — тоже, и в RTL он теперь у правого края.
