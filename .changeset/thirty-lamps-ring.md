---
'@soldy/ui-angular': patch
---

Адаптер проходит AOT-компиляцию (`ngc`): запрос корня `#root` в `TComponentBase` объявлен полем базы. Вызов `viewChild` внутри метода компилятор Angular не распознавал и падал с NG8110, поэтому приложение с компонентами soldy не собиралось.
