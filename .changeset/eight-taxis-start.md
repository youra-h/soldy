---
'@soldy-ui/plugins': patch
---

Spinner: плагин раскладки берёт толщину кольца у итога ядра (`resolvedBorderWidth`). Толщина числом, равным автоматической, теперь переживает смену размера: раньше у `xl` кольцо становилось 2px вместо заданного 1px.
