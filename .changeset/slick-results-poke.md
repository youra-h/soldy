---
'@soldy-ui/plugins': patch
---

TDismissPlugin ведёт открытость владельца той же привязкой, что и остальные плагины оверлея; поведение не изменилось. `IDismissPluginOptions` теперь наследует `IOverlayOpenOptions`: опции `property` и `event` объявлены там, состав опций прежний.
