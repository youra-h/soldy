---
'@soldy-ui/setup': patch
---

Из манифеста убрана точка входа `@soldy-ui/setup/contributions`: папки за ней нет с тех пор, как контракт компонента объявляется прямо в дескрипторе, и импорт по этому пути падал. Всё, что пакет отдаёт, импортируется из `@soldy-ui/setup`.
