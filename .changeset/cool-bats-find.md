---
'@soldy/setup': patch
---

Внутренняя структура пакета разложена по стадиям: имена (naming), описание компонента (define), сборка на монтирование (assemble), регистрации приложения (registry) и связывание с фреймворком (adapter). Публичный экспорт и поведение не изменились.
