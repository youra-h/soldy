---
'@soldy-ui/vue': patch
---

RadioGroup и RadioGroup.Item в режиме разработки рендерят корнем один элемент, как в продакшене. Раньше комментарий над корнем шаблона делал корнем фрагмент «комментарий + элемент»: `$el` компонента был текстовым якорем фрагмента, а `wrapper.element` в `@vue/test-utils` смотрел на родителя.
