---
'@soldy/setup': patch
---

Слой разделён на два: `protected/` — механика (define, naming, registry, adapter), `content/` — наполнение (дескрипторы, расширения адаптера, роли иконок). Публичный API `@soldy/setup` не изменился: всё по-прежнему импортируется из корня пакета.
