---
'@soldy-ui/angular': patch
---

Адаптер Angular выходит собранным. Пакет собирает `ng-packagr` в частичной форме по Angular Package Format — один FESM-модуль и декларации одним файлом, — и компилятор приложения дособирает его компоненты под свою версию Angular. Раньше пакет отдавал исходники (`src/index.ts`), и в чужом проекте он не собирался. Точки входа — `main` и `types`, поля `exports` у манифеста нет.
