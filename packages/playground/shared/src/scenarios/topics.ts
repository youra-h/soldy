import type { TTopic } from './types'

/**
 * Темы страницы тестов в порядке меню.
 *
 * Стенд показывает только темы, в которых у адаптера есть что запустить (см.
 * каталог адаптера): пустой пункт меню ничего не проверяет. Новую тему
 * (`collections`) заводит задача, в которой у неё появятся сценарии.
 */
export const TOPICS: readonly TTopic[] = [
	{
		id: 'events',
		label: 'Events',
		description: 'Какие события компонент отдаёт потребителю и с какими аргументами',
	},
	{
		id: 'slots',
		label: 'Slots',
		description: 'Как содержимое слотов ложится в разметку при разном содержимом и ширине',
	},
]
