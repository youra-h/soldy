/**
 * Публичная граница папки: только `./public`.
 *
 * `./internal` сюда не попадает намеренно — этим и определяется, что часть
 * сборки видна потребителю `@soldy-ui/core`, а часть остаётся рабочим
 * инструментом самих компонентов. См. комментарий в начале `internal.ts`.
 */
export * from './public'
