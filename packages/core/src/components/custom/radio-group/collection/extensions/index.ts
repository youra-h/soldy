/**
 * Внутренние расширения коллекции радио.
 *
 * Расширение одно — `radioGroup`: свойства группы на радио и связь `value`
 * группы с активным радио. Адаптера элемента у него нет: всё, что оно пишет
 * радио, пишется в собственные свойства элемента.
 */

export * from './radio-group'
