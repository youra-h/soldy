/**
 * Внутренние расширения коллекции Collapse.
 *
 * `collapse` — вид и стрелка, `content` — связка «заголовок ↔ панель».
 * Каждое в своей папке, внутри пара «расширение коллекции + item-адаптер».
 */

export * from './collapse'
export * from './content'
