/**
 * Внутренние расширения коллекции Accordion.
 *
 * `accordion` — вид и стрелка, `content` — связка «заголовок ↔ панель».
 * Каждое в своей папке, внутри пара «расширение коллекции + item-адаптер».
 */

export * from './accordion'
export * from './content'
