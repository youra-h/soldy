/**
 * Label — подпись контрола на базе TLabel.
 */

// 1. Base (типы и пропсы)
export * from './base.component'

// 2. Setup (логика и хуки)
export { useSetupLabel } from './setup.component'

// 3. View (JSX-шаблон)
export { Label } from './Label'
