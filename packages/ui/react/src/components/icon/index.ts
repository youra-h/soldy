/**
 * Icon — иконка на базе TIcon: размер, доступное имя.
 */

// 1. Base (типы и пропсы)
export * from './base.component'

// 2. Setup (логика и хуки)
export { useSetupIcon } from './setup.component'

// 3. View (JSX-шаблон)
export { Icon } from './Icon'
