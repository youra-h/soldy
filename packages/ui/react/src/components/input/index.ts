/**
 * Input — текстовое поле на базе TInput со слотами по сторонам.
 */

// 1. Base (типы и пропсы)
export * from './base.component'

// 2. Setup (логика и хуки)
export { useSetupInput } from './setup.component'

// 3. View (JSX-шаблон)
export { Input } from './Input'
