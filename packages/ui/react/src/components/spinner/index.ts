/**
 * Spinner — индикатор загрузки на базе TSpinner.
 */

// 1. Base (типы и пропсы)
export * from './base.component'

// 2. Setup (логика и хуки)
export { useSetupSpinner } from './setup.component'

// 3. View (JSX-шаблон)
export { Spinner } from './Spinner'
