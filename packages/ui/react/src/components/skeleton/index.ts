/**
 * Skeleton — заглушка загрузки на базе TSkeleton.
 */

// 1. Base (типы и пропсы)
export * from './base.component'

// 2. Setup (логика и хуки)
export { useSetupSkeleton } from './setup.component'

// 3. View (JSX-шаблон)
export { Skeleton } from './Skeleton'
