import { TSlideSnapStrategy } from './base.strategy'

/**
 * TNoneSnapStrategy — щелчка нет (`none`): ручка идёт за указателем, метки
 * её не притягивают. Всё нужное уже в общей части.
 */
export class TNoneSnapStrategy extends TSlideSnapStrategy {}
