import type { TModalLayoutPluginEvents } from '../../overlay/modal-layout'

/** События раскладки панели — те же, что у общей раскладки модального слоя. */
export type TDrawerLayoutPluginEvents = TModalLayoutPluginEvents
